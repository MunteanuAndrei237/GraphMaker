import { createTheme } from "@mui/material";
const blueAndGreentTheme = createTheme({
  palette: {
    primary: {
      main: "#1111ff",
    },
    secondary: {
      main: "#19857b",
    },
  },
});

export default blueAndGreentTheme;
